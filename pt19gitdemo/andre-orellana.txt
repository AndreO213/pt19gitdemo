Codepen Assignment:
https://codepen.io/Killua213/pen/abvKVVb

    1. What is Semantic HTML? 
    
    - Semantic HTML are elements that provide meaning

    2. What is HTML used for? 

    - It's used to build the foundation for a website

    3. What is an attribute and where do we put it? 

    - An attribute add more information to an element. You add it after an element within the left and right chevrons.

    4. What is the h1 tag used for? How many times should I use it on a page?

    -H1 tag is used to describe the most important header on the page

    5. Name two tags that have required attributes

    -Anchor tag and img tag

    6. What do we put in the head of our HTML document? 

    - <html>

    7. What is an id? 

    - a unique identifier

    8. What elements can I add an id to? 

    - Any element 

    9. How many times can I use the same id on a page? 

    - Once

    10. What is a class? 

    - An attribute

    11. What elements can I add a class to? 

    - Any

    12. How many times can I use the same class on a page? 

    - As many as needed

    13. How do I get my link to open in a new tab?

    - Right click on the link and select open in new tab

    14. What is the alt attribute in the image tag used for? 

    - to describe what the image is to someone that is not able to see the image

    15. How do I reference an id?

    - Using the # in css

    16. What is the difference between a section and a div

    - Sections are semantic and let people know the different sections whereas divs are only used to divide up the code to organize but do not have any special meaning it is only presentational

    17. What is CSS used for?

    - CSS is used to tstyle the html or for visuals and positioning only

    18. How to we select an element? Example - every h2 on the page

    - Simply type in h2 and use the { } brackets 

    19. What is the difference between a class and an id? - Give me an example of when I might use each one

    - Class would be used for multiple divs that correspond with each other and id would be used for a unique section that will not repeat itself 

    20. How do we select classes in CSS?
    
    - With a . infornt of the class name

    21. How do we select a p element with a single class of “human””?

    - .human, p

    22. What is a parent child selector? When would this be useful? 

    - > , parent child selectors are used for specificity

    23. How do you select all links within a div with the class of sidebar?

    - You use .sidebar and the name of the element that all the links are associated with

    24. What is a pseudo selector?

    - A pseudo selector can specify which content you want to change in a list or set of elements with the same tag

    25. What do we use the change the spacing between lines?

    - Line-height: (value)

    26. What do we use to change the spacing between letters?

    - Letter-spacing:(value)

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?

    - Text-transform

    28. How do I add a 1px border around my div that is dotted and black?
    
    - border: 1px dotted black;

    29. How do I select everything on the page? 

    - By using the universal selector *

    30. How do I write a comment in CSS?

    - By typing /* Comment */

    31. How do I find out what file I am in, when I am using the command line? 

    - You can use the pwd command

    32. Using the command line - how do I see a list of files/folders in my current folder?

    - The ls command

    33. How do I remove a file via the command line? Why do I have to be careful with this? 

    - The rm or -r command can be dangerousbecause there are no safeguards in place to prevent it from deleting a directory or file

    34. Why should I use version control? 

    - To speak directly to your computer

    35. How often should I commit to github?

    - Multiple times in a day

    36. What is the command we would use to push our repo up to github? 

    - The git commit -m with your description

    37. Walk me through Lambda's git flow. 

    First you always fork.
    Add your TL as a collaborator.
    Then clone it and copy the HTTPS link.
    Type in git clone into your terminal followed by pasting the HTTPS link.
    Once cloned you creat your branch by typing git checkout and your branch name
    When ready to push you type in git push -u origin and your branch name
    Then on GitHub you will pull the request and add your TL as a reviewer

Stretch Questions

    1. What is the difference between an inline element and a block element?

    - Inline elements take up space that is needed only and block elements take up a whole line even if the content is tiny

    2. What happens when an element is positioned absolutely?

    - It becomes fixed and will not change

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

    - You can use the inline-block value

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

    - Div, p, footer. A, span. button.

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them?

    - The box model consists of margin border padding and the content within the box. The way to make sure all the parts of the box model move at the same time is with the box-sizing value